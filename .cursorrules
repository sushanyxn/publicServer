Project Rules for this Repo

> `.cursor/MEMORY.md` 是架构与流程的详细事实来源；本文件是编码规范摘要，两者冲突时以 MEMORY 为准。
> 详细规范已拆分到 `.cursor/rules/` 下的多个规则文件中，按 glob 模式自动注入。

## 技术栈

- **语言**: Java 21 | **构建**: Maven | **框架**: Spring Boot 3.3.0
- **入口类**: `GameMain`（游戏服）、`SceneMain`（场景服）、`SingleStartMain`（合并启动）
- **模块类型**: SLG 策略游戏服务端（多进程架构：game + scene，可合并启动）

## 规范索引

| 规则文件 | 内容 | 作用范围 |
|----------|------|---------|
| `rules/general.md` | 通用编码规范、AI 使用规则、安全与配置 | 全局 |
| `rules/protocol.md` | 协议定义、命名、message.yml、Facade | `**/message/**`, `**/facade/**` |
| `rules/executor.md` | 执行器、虚拟线程、定时任务 | `**/executor/**`, `**/task/**` |
| `rules/logging.md` | 日志框架与级别 | 全局 |
| `rules/persistence.md` | MySQL/Redis/持久化 | `**/entity/**`, `**/cache/**`, `**/redis/**` |
| `rules/singlestart.md` | 合并启动模块 | `**/singlestart/**` |
| `rules/progress.md` | 进度管理系统 | `**/progress/**` |

## 模块层级

- **一级**: `slg-common`
- **二级**: `slg-net`、`slg-redis`、`slg-support`
- **三级**: `slg-shared-modules`、`slg-game`、`slg-scene`、`slg-robot`、`slg-web`、`slg-log`
- **四级**: `slg-singlestart`
- **禁止越级依赖**，详见 `.cursor/MEMORY.md`

## 关键约定速览

- **日志**: 使用 `LoggerUtil`，业务层禁止直接使用 SLF4J Logger
- **执行器**: 统一通过 `Executor.Xxx` 调用，禁止自建线程池，虚拟线程中禁止 `synchronized`
- **协议**: CM_/SM_/IM_ 前缀，必须在 message.yml 注册，Facade 使用 @MessageHandler
- **持久化**: 禁止 JpaRepository，禁止直接操作 EntityManager/RedisTemplate
- **工厂方法**: 统一 `valueOf`
- **Lombok**: 优先使用以减少样板代码
