# 复制到 Cursor 用户规则（个人规则）

请将下面一行复制到 **Cursor Settings → Rules → User Rules**（或 **File → Preferences → Settings** 中的 Rules for AI），以便在本机所有项目中统一 JavaDoc 作者。

```
JavaDoc 类注释中的 @author 统一使用 yangxunan。
```

设置后，AI 在生成或补全类注释时会使用该作者名。
